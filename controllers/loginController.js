const {pool} = require('../api/db.js');

const validarSesion = async (req, res) => {
    try {
        const {nickname, contraseña} = req.dody;
    } catch (error) {
        
    }
};

module.exports.validarSesion = validarSesion;